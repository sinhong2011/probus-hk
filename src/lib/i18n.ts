import * as m from "~/paraglide/messages";
import type { Bilingual } from "~/data/types";

export type Lang = "zh" | "en";

/**
 * UI copy is compiled by Paraglide from messages/{locale}.json into plain
 * functions. Paraglide was chosen over a runtime i18n library because it has no
 * framework coupling at all - the Solid bindings of the alternatives still
 * target Solid 1 and import module paths that Solid 2 removed.
 *
 * The locale is always passed explicitly rather than read from Paraglide own
 * ambient state. That is deliberate: reading the language signal at the call
 * site is what makes a label re-render when the user switches language, with no
 * page reload and no provider component.
 */
type Message = (inputs?: Record<string, never>, options?: { locale?: Lang }) => string;

const MESSAGES = {
  nearby: m.nearby,
  home: m.home,
  popularRoutes: m.popular_routes,
  popularRoutesHint: m.popular_routes_hint,
  search: m.search,
  searchRoutes: m.search_routes,
  saved: m.saved,
  settings: m.settings,
  more: m.more,
  pinned: m.pinned,
  nearbyStops: m.nearby_stops,
  routesHere: m.routes_here,
  routes: m.routes,
  towards: m.towards,
  arriving: m.arriving,
  scheduled: m.scheduled,
  noService: m.no_service,
  minute: m.minute,
  live: m.live,
  passed: m.passed,
  youAreHere: m.you_are_here,
  walk: m.walk,
  reverse: m.reverse,
  wholeFare: m.whole_fare,
  holidayFare: m.holiday_fare,
  weekdayFare: m.weekday_fare,
  stops: m.stops,
  aboutMinutes: m.about_minutes,
  language: m.language,
  theme: m.theme,
  themeAuto: m.theme_auto,
  themeLight: m.theme_light,
  themeDark: m.theme_dark,
  refresh: m.refresh,
  radius: m.radius,
  searchRange: m.search_range,
  searchRangeHint: m.search_range_hint,
  showScheduled: m.show_scheduled,
  reduceMotion: m.reduce_motion,
  offlineData: m.offline_data,
  routeDatabase: m.route_database,
  updateNow: m.update_now,
  downloaded: m.downloaded,
  updatedAt: m.updated_at,
  locating: m.locating,
  locationDenied: m.location_denied,
  locationHint: m.location_hint,
  noResults: m.no_results,
  noNearby: m.no_nearby,
  emptySaved: m.empty_saved,
  emptySavedHint: m.empty_saved_hint,
  loadingData: m.loading_data,
  dataError: m.data_error,
  dataErrorHint: m.data_error_hint,
  offlineTitle: m.offline_title,
  offlineHint: m.offline_hint,
  waitingForNetwork: m.waiting_for_network,
  backOnline: m.back_online,
  retryAuto: m.retry_auto,
  retrying: m.retrying,
  retry: m.retry,
  crashTitle: m.crash_title,
  crashHint: m.crash_hint,
  reload: m.reload,
  goHome: m.go_home,
  notFoundTitle: m.not_found_title,
  notFoundHint: m.not_found_hint,
  notFoundRoute: m.not_found_route,
  notFoundStop: m.not_found_stop,
  notFoundLine: m.not_found_line,
  errorDetails: m.error_details,
  copyDetails: m.copy_details,
  detailsCopied: m.details_copied,
  dimmedKeys: m.dimmed_keys,
  joint: m.joint,
  edit: m.edit,
  done: m.done,
  suspended: m.suspended,
  noLiveFeed: m.no_live_feed,
  browseRoutes: m.browse_routes,
  categories: m.categories,
  frequent: m.frequent,
  noFrequent: m.no_frequent,
  noFrequentHint: m.no_frequent_hint,
  recent: m.recent,
  removeRecent: m.remove_recent,
  allKinds: m.all_kinds,
  allLines: m.all_lines,
  kindBus: m.kind_bus,
  kindMinibus: m.kind_minibus,
  kindRail: m.kind_rail,
  kindFerry: m.kind_ferry,
  clearQuery: m.clear_query,
  noRecent: m.no_recent,
  noRecentHint: m.no_recent_hint,
  moreRoutes: m.more_routes,
  railJourney: m.rail_journey,
  railJourneyHint: m.rail_journey_hint,
  fromStation: m.from_station,
  toStation: m.to_station,
  chooseStation: m.choose_station,
  twoChanges: m.two_changes,
  changeWalk: m.change_walk,
  suggestedRoute: m.suggested_route,
  otherRoutes: m.other_routes,
  walkTo: m.walk_to,
  rideTo: m.ride_to,
  swapEnds: m.swap_ends,
  searchAnything: m.search_anything,
  fareConcession: m.fare_concession,
  routesCount: m.routes_count,
  firstBus: m.first_bus,
  lastBus: m.last_bus,
  alreadyLeft: m.already_left,
  locationBlocked: m.location_blocked,
  locationBlockedHint: m.location_blocked_hint,
  locationInsecure: m.location_insecure,
  locationInsecureHint: m.location_insecure_hint,
  locationUnsupported: m.location_unsupported,
  locationTimeout: m.location_timeout,
  locationTimeoutHint: m.location_timeout_hint,
  boardHere: m.board_here,
  alightHere: m.alight_here,
  pickAlight: m.pick_alight,
  rideTime: m.ride_time,
  boardLabel: m.board_label,
  alightLabel: m.alight_label,
  arriveAbout: m.arrive_about,
  clearTrip: m.clear_trip,
  headingOut: m.heading_out,
  savedTrips: m.saved_trips,
  waitLabel: m.wait_label,
  saveTrip: m.save_trip,
  noticesAffecting: m.notices_affecting,
  noNoticesYours: m.no_notices_yours,
  display: m.display,
  liveEta: m.live_eta,
  share: m.share,
  linkCopied: m.link_copied,
  every: m.every,
  stopsMatched: m.stops_matched,
  viewAll: m.view_all,
  fareOctopus: m.fare_octopus,
  fareFull: m.fare_full,
  fareOctopusAdult: m.fare_octopus_adult,
  fareSingleTicket: m.fare_single_ticket,
  fareChild: m.fare_child,
  fareElderly: m.fare_elderly,
  fareStudent: m.fare_student,
  loadingFares: m.loading_fares,
  tapForEta: m.tap_for_eta,
  earlierStops: m.earlier_stops,
  openStop: m.open_stop,
  mapUnavailable: m.map_unavailable,
  plan: m.plan,
  notices: m.notices,
  fromLabel: m.from_label,
  toLabel: m.to_label,
  myLocation: m.my_location,
  chooseDest: m.choose_dest,
  planHint: m.plan_hint,
  direct: m.direct,
  oneChange: m.one_change,
  changeHere: m.change_here,
  wholeJourney: m.whole_journey,
  useThisStop: m.use_this_stop,
  noStopNearPin: m.no_stop_near_pin,
  noJourneys: m.no_journeys,
  noJourneysHint: m.no_journeys_hint,
  noNotices: m.no_notices,
  noNoticesHint: m.no_notices_hint,
  noticesSource: m.notices_source,
  walkLabel: m.walk_label,
  boardAt: m.board_at,
  alightAt: m.alight_at,
  affectsRoutes: m.affects_routes,
  noticesFailed: m.notices_failed,
  leaveNow: m.leave_now,
  leaveIn: m.leave_in,
  sortManualHint: m.sort_manual_hint,
  sortEtaHint: m.sort_eta_hint,
  sortDistanceHint: m.sort_distance_hint,
  sortRouteHint: m.sort_route_hint,
  tooLate: m.too_late,
  takeNext: m.take_next,
  timetable: m.timetable,
  daysDaily: m.days_daily,
  daysWeekday: m.days_weekday,
  daysSaturday: m.days_saturday,
  daysSunday: m.days_sunday,
  everyMinutes: m.every_minutes,
  singleDeparture: m.single_departure,
  noTimetable: m.no_timetable,
  routeInfo: m.route_info,
  navigation: m.navigation,
  close: m.close,
  rail: m.rail,
  platform: m.platform,
  cars: m.cars,
  nearestStop: m.nearest_stop,
  chooseOrigin: m.choose_origin,
  mapMyLocation: m.map_my_location,
  mapWholeRoute: m.map_whole_route,
  mapExpand: m.map_expand,
  mapCollapse: m.map_collapse,
  busArrivingNow: m.bus_arriving_now,
  busOneStopAway: m.bus_one_stop_away,
  busAwayLead: m.bus_away_lead,
  busAwayTail: m.bus_away_tail,
  busFinding: m.bus_finding,
  busNoneRunning: m.bus_none_running,
  busTimetableOnly: m.bus_timetable_only,
  busNoLiveFeed: m.bus_no_live_feed,
  busFetchFailed: m.bus_fetch_failed,
  busUnplaceable: m.bus_unplaceable,
  mapSheet: m.map_sheet,
  mapTapStop: m.map_tap_stop,
  mapFindBus: m.map_find_bus,
  mapEstimated: m.map_estimated,
  mapGestureMobile: m.map_gesture_mobile,
  mapGestureDesktop: m.map_gesture_desktop,
  mapGestureMac: m.map_gesture_mac,
  walkMinutes: m.walk_minutes,
  notRunning: m.not_running,
  bookmarkThese: m.bookmark_these,
  addBookmark: m.add_bookmark,
  bookmarked: m.bookmarked,
  noLocation: m.no_location,
  byStop: m.by_stop,
  allRoutes: m.all_routes,
  nearbyRoutes: m.nearby_routes,
  alerts: m.alerts,
  remindMe: m.remind_me,
  alertArrival: m.alert_arrival,
  alertArrivalHint: m.alert_arrival_hint,
  alertDestination: m.alert_destination,
  alertDestinationHint: m.alert_destination_hint,
  alertLead: m.alert_lead,
  alertRadius: m.alert_radius,
  alertOn: m.alert_on,
  alertOff: m.alert_off,
  alertFiredArrival: m.alert_fired_arrival,
  alertFiredDest: m.alert_fired_dest,
  alertPermission: m.alert_permission,
  alertPermissionHint: m.alert_permission_hint,
  alertBlocked: m.alert_blocked,
  alertUnsupported: m.alert_unsupported,
  alertEnable: m.alert_enable,
  alertEnabled: m.alert_enabled,
  noAlerts: m.no_alerts,
  noAlertsHint: m.no_alerts_hint,
  filter: m.filter,
  allItems: m.all_items,
  sortBy: m.sort_by,
  sortManual: m.sort_manual,
  sortEta: m.sort_eta,
  sortDistance: m.sort_distance,
  sortRoute: m.sort_route,
  sortFare: m.sort_fare,
  sortOperator: m.sort_operator,
  groupLabel: m.group_label,
  routeNumber: m.route_number,
  collapseNav: m.collapse_nav,
  expandNav: m.expand_nav,
  ungrouped: m.ungrouped,
  newGroup: m.new_group,
  groupName: m.group_name,
  noGroup: m.no_group,
  changeStop: m.change_stop,
  pinTop: m.pin_top,
  unpinTop: m.unpin_top,
  pinnedTop: m.pinned_top,
  saveLabel: m.save_label,
  nothingInFilter: m.nothing_in_filter,
  published: m.published,
  noticesUpdated: m.notices_updated,
  nextTrains: m.next_trains,
  interchange: m.interchange,
  lineStations: m.line_stations,
  wholeLine: m.whole_line,
  tapStation: m.tap_station,
  tapDestination: m.tap_destination,
  terminus: m.terminus,
  firstTrain: m.first_train,
  lastTrain: m.last_train,
  lineMap: m.line_map,
  appName: m.app_name,
  about: m.about,
  appTagline: m.app_tagline,
  versionLabel: m.version_label,
  sourceCode: m.source_code,
  sourceCodeHint: m.source_code_hint,
  shareApp: m.share_app,
  shareAppHint: m.share_app_hint,
  creditLive: m.credit_live,
  creditRoutes: m.credit_routes,
  creditNotices: m.credit_notices,
  creditMap: m.credit_map,
  creditIcons: m.credit_icons,
  privacyNote: m.privacy_note,
  notAffiliated: m.not_affiliated,
  dataSources: m.data_sources,
  networkMap: m.network_map,
  networkMapHint: m.network_map_hint,
  zoomIn: m.zoom_in,
  zoomOut: m.zoom_out,
  wholeNetwork: m.whole_network,
  noStationPicked: m.no_station_picked,
  linesHere: m.lines_here,
  serviceNotice: m.service_notice,
  noticeFromOperator: m.notice_from_operator,
  trafficCamera: m.traffic_camera,
  cameraFailed: m.camera_failed,
  cameraSource: m.camera_source,
  specialDepartures: m.special_departures,
} as const satisfies Record<string, Message>;

export type MessageKey = keyof typeof MESSAGES;

export function t(key: MessageKey, lang: Lang): string {
  return MESSAGES[key]({}, { locale: lang });
}

/**
 * Hong Kong transit is read bilingually in practice - a Chinese-first rider
 * still recognises the English name on the bus blind - so most rows show both,
 * and these helpers pick the right half of a value from the route database.
 */
export function pick(value: Bilingual | undefined, lang: Lang): string {
  return value ? value[lang] : "";
}

/**
 * KMB stop names carry a pole code such as "(WT916)". It is useful when you are
 * standing at a stop served by several poles, but it is noise in a dense list.
 */
export function stripStopCode(name: string): string {
  return name.replace(/\s*\([A-Z]{2}\d{3,4}\)\s*$/, "").trim();
}

export function stopCode(name: string): string | null {
  return /\(([A-Z]{2}\d{3,4})\)\s*$/.exec(name)?.[1] ?? null;
}
