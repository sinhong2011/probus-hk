node_modules/
dist/
shots/
test-results/
playwright-report/
.playwright-mcp/
# Compiled by Paraglide from messages/*.json at build time.
src/paraglide/
.inlang/

# Design canvases: the seeded pages embed a ~2 MB editor payload each, and the
# published artifact is the shareable copy.
design/

# Locally generated development certificate; see `npm run cert`.
.certs/

# Screenshots dropped in the working directory while checking a change - the
# same thing `shots/` and `.playwright-mcp/` above hold, just loose. Only the
# root: the app's own images live under `public/`.
/*.png
